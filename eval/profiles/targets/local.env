# Target profile: LOCAL — Irene running on this machine.
# Sourced by the Makefile (and shell). Override any value per-run, e.g.:
#   make ws TARGET=local WS_AUDIO_URL=ws://localhost:7000/ws/audio
WS_AUDIO_URL=ws://localhost:6000/ws/audio
HEALTH_URL=http://localhost:6000/health
MQTT_HOST=localhost
MQTT_PORT=1883

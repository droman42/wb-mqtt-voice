# Config-under-test: full voice assistant. Used only for LOCAL bring-up (`make serve`/`compare`);
# selects which config Irene launches with. The eval suite itself is config-agnostic.
IRENE_CONFIG_FILE=configs/voice.toml

# Config-under-test: ARBITRARY — for testing a different model/profile ad hoc.
# Copy to custom.env (git-ignored), point it at any TOML, then:
#   make serve CONFIG=custom        # bring Irene up locally with it
#   make ws    TARGET=local CONFIG=custom
IRENE_CONFIG_FILE=/absolute/path/to/experiment.toml
